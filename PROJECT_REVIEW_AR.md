# مراجعة DZ Commerce وخطة تطويره

> تحديث 8 سبتمبر 2026: نُفذت معالجة الأمان والمزامنة الأساسية الواردة في هذه المراجعة: جلسة خادم موقعة، قراءة Google Sheets عبر WIF للقراءة فقط، إغلاق المشاركة العامة، مزامنة مجزأة ونشر ذري، وتجديد JWT تلقائي مختبر. تبقى الفقرات أدناه سجلاً لحالة المشروع عند بداية التدقيق.

تاريخ المراجعة: 6 سبتمبر 2026.

تحديث بعد مراجعة Apps Script الفعلي وسجلاته: تم التحقق لاحقاً من مسار API إلى Sheets، وسبب انتهاء مهلة المزامنة، ومصادر أعمدة التاريخ والمبلغ والشحن. النتائج الأحدث وخطة إصلاح المسار في [خطة تطوير المزامنة](./SYNC_DEVELOPMENT_PLAN_AR.md). حدود التحقق المذكورة أدناه تصف المراجعة الأولى.

## النطاق والنتيجة

راجعت المشروع الأساسي في `F:/dz-dashboard`: الواجهات، جلب البيانات، الحسابات، تسجيل الدخول، Telegram، التخزين المحلي، الاختبارات وإعدادات البناء والنشر. عاينت لوحة التحكم المنشورة في المتصفح بصرياً ومن خلال عناصر الصفحة. لم أعدّل كود التطبيق أو إعدادات الخدمات، ولم أرسل رسائل Telegram.

المشروع أساس مفيد للمتابعة والتحليل: React وTypeScript، واجهة عربية RTL، تقارير وفلاتر ورسوم، فصل نسبي للحسابات عن الصفحات، واختبارات قائمة. لكن الاعتماد التشغيلي عليه يحتاج أولاً إصلاح الحماية وصحة المؤشرات. إضافة صفحات جديدة قبل هذا ستوسع نفس المشاكل.

لم أتحقق من صلاحيات مشاركة Google Sheet، أسرار Vercel، إعدادات المشروع الموجود في Supabase، أو مسار نقل بيانات Octomatic إلى Sheets. لا يوجد ربط مباشر بـOctomatic أو Supabase في الكود الأساسي الذي بحثت فيه؛ وجود تبويبات لهذه الخدمات لا يثبت تكاملها. لم أنفذ اختبار اختراق أو تدقيق ثغرات حزم شامل، ولم أختبر كل صفحة على أجهزة متعددة. النسخة المنشورة لم تتم مطابقتها مع commit محدد.

## نتائج التحقق

| الفحص | النتيجة |
|---|---|
| TypeScript للواجهة وإعدادات Vite | ناجح |
| TypeScript لـAPI بشكل مستقل | ناجح؛ غير مشمول حالياً في مراجع build الرئيسية |
| Vite production build | ناجح؛ JavaScript رئيسي 608.14 kB، مضغوط 182.17 kB |
| اختبارات المشروع الأساسي فقط | 49 اختباراً ناجحاً في 5 ملفات |
| الاختبارات بالبحث الافتراضي | 98 اختباراً في 10 ملفات؛ تشمل نسخة `dz-commerce` الداخلية |
| ESLint للمصدر الأساسي وAPI وVite | 46 خطأ |
| ESLint الافتراضي للمجلد كله | 86 خطأ؛ يشمل النسخة الداخلية |

أمر npm المحلي متعطل بسبب مسار `npm-cli.js` مفقود في تثبيت الجهاز. شغّلت الأدوات المثبتة مباشرة بواسطة Node؛ هذا عطل في بيئة الأوامر وليس فشلاً في بناء التطبيق. لم أغيّر تثبيت الجهاز. أعاد فحص البناء توليد مجلد dist المحلي.

## الأولوية الأولى: الحماية

1. **تسجيل الدخول لا يحمي البيانات على الخادم.** `src/lib/auth.ts:26` يقارن بكلمة مرور `VITE_DASHBOARD_PASSWORD` داخل المتصفح. متغيرات VITE المستخدَمة في كود العميل تدخل في الحزمة عند البناء. كذلك `isAuthenticated` يقبل بيانات جلسة دون التحقق من `authenticated` أو صلاحية بنية `expiresAt`. الحل: حسابات وجلسات موثقة من الخادم، وصلاحيات تُفرض عند قراءة البيانات، وإغلاق الوصول المباشر غير المصرح به إلى المصدر. [توثيق Vite](https://vite.dev/guide/env-and-mode).

2. **قراءة Sheets مستقلة عن جلسة التطبيق.** `src/lib/sheetsApi.ts` يقرأ gviz مباشرة دون تفويض التطبيق، ويسجل عينات صفوف في console. كلمة مرور الواجهة لا تضيف حماية إلى رابط المصدر. يلزم فحص صلاحيات الشيت ونقل القراءة إلى خدمة موثقة، وحذف سجلات بيانات العملاء من إصدار الإنتاج.

3. **Webhook Telegram غير موثّق المصدر.** `api/telegram-bot.ts:686` لا يتحقق من secret header؛ يعتمد على `message.from.id` القادم في JSON. مع غياب `TELEGRAM_ALLOWED_ID` يسمح بمرور الجميع. يلزم رفض التشغيل عند نقص الإعدادات، والتحقق من secret Telegram، ومن المرسل والمحادثة المستهدفة، ومن بنية التحديث، وحفظ `update_id` لمنع التكرار. سكريبت setup لا يسجل `secret_token` حالياً. لم أجرّب إرسال طلب مزور على الخدمة الحية. [توثيق Telegram](https://core.telegram.org/bots/api#setwebhook).

4. **Token البوت محفوظ في المتصفح.** `src/lib/telegramNotifier.ts` وواجهة الإعدادات يخزنان السر في localStorage ويرسلان إلى Telegram مباشرة. ينقل السر إلى الخادم؛ يحتفظ العميل بإعدادات الإشعارات فقط. مهلة الست ساعات محفوظة في Map مؤقتة وتضيع بعد إعادة تحميل الصفحة؛ يلزم تخزين دائم على الخادم.

## الأولوية الثانية: صحة البيانات والحسابات

| المشكلة المؤكدة | الدليل والأثر | الإصلاح المطلوب |
|---|---|---|
| نسب نمو ثابتة | `src/pages/Dashboard.tsx:167` وما بعدها: 2.3 و1.1 و0.8 و1.5 قيم مكتوبة مباشرة | حساب المقارنة مع فترة سابقة مساوية؛ عرض «لا توجد مقارنة» عند نقص البيانات |
| إجمالي الإيراد يجمع حالات غير مسلّمة | `getTrackingMetrics` يجمع total لكل التتبع، بينما بطاقة الفترة تستعمل المسلّم | تسمية الأول «قيمة الطلبات»، وفصل قيمة المسلّم والمبلغ المحصّل فعلياً |
| اختلاف في صافي الربح | `src/lib/financialEngine.ts:227` يضيف شحن المرتجعات للتكلفة؛ `trueNetProfit` لا يخصمه | توحيد معادلة الربح ومكوناتها وربط كل البطاقات والقرارات بها |
| نسبة تسليم سنوية خاطئة | `src/lib/dashboardMetrics.ts:418` يحسب `(orders-returned)/orders` فيحتسب الطلب الجاري كمسلّم | حفظ عدد المسلّم صراحة واستعمال تعريف موحد للمعدل |
| متوسط سنوي غير موزون | `getYearComparison` يعطي كل شهر نفس الوزن مهما اختلف عدد الطلبات | حساب النسبة من مجموع المسلّم والمرتجع؛ أو تسمية الرقم بوضوح «متوسط نسب الأشهر» |
| تصنيف الحالة بتطابق جزئي | `src/lib/sheetsApi.ts`: «non livré» يصبح delivered، و«en cours de livraison» يصبح transit | جدول حالات موحد واضح، مع فئة unknown للحالات غير المعروفة وتنبيه لجودة البيانات |
| اختلاف لغة الحالات | `normalizeStatus('Confirmed')` يعيد Unknown؛ بعض فلاتر Dashboard تقارن الحالة الخام بقيم إنجليزية | تطبيع الحالات مرة واحدة عند الاستيراد مع الاحتفاظ بالنص الأصلي |
| اختلاط تاريخ الطلب بتاريخ الحدث | نوع TrackingOrder يحتوي date واحدة تستعمل لتقارير التسليم اليومية | حقول مستقلة: created_at وshipped_at وdelivered_at وreturned_at وcollected_at؛ تأكيد معنى عمود Date الحالي قبل تعديل التقارير |
| تواريخ محلية وUTC مختلطة | `getDateISOString` يستعمل UTC، ومواضع أخرى تستعمل التوقيت المحلي وnew Date على نص من Sheets | تحويل صريح لصيغ المصدر واعتماد Africa/Algiers لتجميع أيام الأعمال |
| حدود الفترات | أزرار آخر 7/30 يوماً تطرح 7/30 وتضم اليومين، فتشمل 8/31 تاريخاً | تحديد تعريف المدة واختبار بداية ونهاية الفترة |

**أمثلة اصطناعية شغلتها على الدوال الفعلية، دون بيانات العملاء:**

- طلب مسلّم وطلب مرتجع بقيمة 5000 دج لكل منهما، وشحن 500 وتكلفة وحدة 1000: `netProfit=2000` بينما `trueNetProfit=2500`. الفرق يساوي شحن المرتجع 500.
- منتج فيه طلب واحد في transit: التقرير السنوي يعيد معدل تسليم 100%.
- `assessOrderRisk('عين قزام', 50000)` يعيد 118 مع عرض المؤشر كمقياس للمخاطر؛ يحتاج نطاقاً مضبوطاً وتفسيراً موحداً.

يلزم أيضاً توضيح معنى عمود Delivery: هل هو رسوم يدفعها العميل أم تكلفة تدفع لشركة الشحن؟ الكود يستعمله كتكلفة ويتيح إضافة تكلفة شحن أخرى. كما يحتسب تكلفة المنتج كاملة على المرتجع؛ يجب تمييز سلعة رجعت للمخزون عن سلعة تالفة. هذه افتراضات تجارية تحتاج تعريفاً موثقاً، وليست كلها أخطاء يمكن حسمها من الكود.

## الاعتمادية والتنبيهات والذكاء التحليلي

- `useSheetData` و`useTrackingData` يخفيان أخطاء الاتصال. أول فشل قد يظهر كصفر طلبات، والفشل اللاحق يترك أرقاماً قديمة دون تنبيه. أضف حالات error وstale، وآخر مزامنة ناجحة لكل مصدر، وإعادة محاولة محدودة ومهلة وإلغاء للطلبات.
- زر التحديث في `src/App.tsx` يحدث Orders فقط؛ التتبع ينتظر تحديثه الدوري. اجعل تحديث المستخدم ينتظر المصدرين ويعرض نتيجة كل واحد.
- المصدران يُحمّلان بالكامل كل دقيقة لكل مستخدم؛ الصفحات مرقمة في العرض فقط. أضف مزامنة مركزية، وتجميعاً وخادماً للصفحات عند زيادة الحجم، وذاكرة مؤقتة مشتركة.
- التاريخ اليومي محفوظ في localStorage مع حذف بعد 90 يوماً. الحفظ الآلي الساعة 22 يعتمد على بقاء الصفحة مفتوحة. استعمل مهمة على الخادم وحفظاً دائماً بقيد يمنع تكرار اليوم والمتجر، مع آلية تعويض الأيام الفائتة.
- الحفظ اليدوي يسمح بأكثر من snapshot لنفس اليوم؛ المتوسطات تأخذ آخر 7/30 سجلاً وليس بالضرورة أياماً متتالية. اجمع أياماً فريدة وعرّف التعامل مع الأيام المفقودة، ووحّد مقام معدل التسليم مع باقي التقارير.
- `storageManager` ينظف بعد الكتابة؛ امتلاء حصة localStorage يمكن أن يفشل قبل التنظيف. يلزم معالجة QuotaExceededError وتفريغ مساحة قبل الكتابة، إلى حين النقل للخادم.
- البحث عن `sendNotification` وجد استعمال الإرسال في زر تجربة الإعدادات فقط. أسماء تنبيهات المخاطر والمخزون المعروضة لا تعني وجود تشغيل آلي لها. يلزم قواعد وعتبات وجدولة وسجل محاولات وإعادة إرسال مع منع التكرار.
- `aiRiskEngine.ts` يستعمل أوزان ولايات ثابتة ومتوسطات حسابية؛ درجات الثقة 25/45/65/90 تعتمد على عدد snapshots فقط. عرضه حالياً كتقدير بقواعد أوضح، ثم معايرة القواعد على النتائج الفعلية واختبارها على فترة مستقلة. بيانات الولايات المعروضة في اللوحة تشمل أسماء فرنسية، بينما خريطة المخاطر عربية؛ يلزم معرف موحد للولاية وإلا تستعمل الدرجة الافتراضية.
- تحليل المنتج قد يقترح التوسعة بعينة صغيرة، أو التوقف مع غياب معلومات كافية. اجعل «بيانات غير كافية» حالة مستقلة قبل أي توصية تشغيلية.

## تحسين تجربة الاستخدام

الواجهة الحالية مفهومة بصرياً وتدعم RTL والوضع الداكن. القائمة فيها 12 وجهة، منها مركزا مخاطر وتقارير متفرقة؛ في المعاينة على نافذة ضيقة تستهلك القائمة مساحة معتبرة. هذه مقترحات تنظيم، وليست نتيجة اختبار أجهزة شامل:

1. تنظيم القائمة إلى: نظرة عامة، العمليات، الربحية، التقارير، الإعدادات؛ جمع مركزي المخاطر.
2. الصفحة الأولى تعرض أرقام الفترة المحددة مع تعريفها: طلبات جديدة، مسلّم، مرتجع، نسبة تسليم محسومة، قيمة المسلّم، صافي الربح بعد التكاليف المعتمدة. تُفصل المؤشرات التراكمية بوضوح.
3. إضافة «ما الذي يحتاج تدخلك اليوم؟»: طلبات تأخرت، طلبات دون متابعة، اختلافات مزامنة، مبالغ شحن لم تُحصّل.
4. صفحة تفاصيل الطلب: مسار الحالات بتواريخها، رقم التتبع، الوكيل، محاولات الاتصال، ملاحظات، وتكلفة وربح الطلب. الكتابة تتطلب تحديد مصدر الحقيقة أولاً.
5. فلاتر الفترة والوكيل والولاية والمنتج وشركة التوصيل تُحفظ في URL لتبادل نفس التقرير؛ فصل المنتج الأساسي عن اللون والمقاس باستخدام SKU وvariant.
6. حالات واضحة لعدم وجود نتائج وفشل التحميل وغياب التكاليف؛ القيم الناقصة لا تتحول تلقائياً إلى أرباح مؤكدة.
7. في النوافذ المنبثقة: إدارة التركيز، role وaria-modal، اسم لزر الإغلاق، واستعادة التركيز بعد الإغلاق. الجدول والترتيب يحتاجان دعماً للوحة المفاتيح.
8. تصدير CSV الحالي يضع النص بين علامات اقتباس دون تهريب الاقتباسات الداخلية. يلزم تهريب صحيح ومنع تفسير مدخلات المستخدم كصيغ في برامج الجداول.

## التصميم التقني المقترح

الاحتفاظ بواجهة React الحالية مع إضافة طبقة بيانات موثوقة تدريجياً:

`المصدر التجاري / Sheets → خدمة مزامنة → PostgreSQL → واجهة بصلاحيات`

`قاعدة البيانات → مهام مجدولة → snapshots وتنبيهات Telegram`

Supabase خيار مناسب للتقييم لأنه يجمع PostgreSQL وتسجيل الدخول وسياسات RLS التي تقيد الصفوف حسب المستخدم. يجب اختبار سياسات القراءة والكتابة فعلياً؛ مجرد تسجيل الدخول لا يكفي. لم أفحص إعدادات حسابك أو أربط الخدمة. [توثيق Auth وRLS](https://supabase.com/docs/guides/database/postgres/row-level-security).

الكيانات المقترحة: orders، order_items، products، variants، agents، shipments، shipment_events، expenses، cod_settlements، daily_snapshots، notification_events، sync_runs. يمكن إضافة stores وmemberships إذا كان الهدف عدة متاجر؛ لا حاجة لفرض تعقيد تعدد المتاجر على استعمال فردي.

تبدأ المزامنة من Sheets للحفاظ على المسار الحالي، ثم يُدرس الربط الرسمي بـOctomatic وشركات التوصيل حسب إمكاناتها وصلاحياتك. الحفظ يكون idempotent: إعادة نفس الطلب أو الحدث لا تنشئ نسخة جديدة. لكل سجل معرف مصدر، ولكل مزامنة وقت ونتيجة وعدد سجلات وأخطاء.

تنظيم الكود: وحدة مشتركة للحالات والتواريخ والحسابات يستعملها التطبيق والبوت، وفصل ملفات financialEngine وdashboardMetrics حسب المجال، وتقسيم الصفحات الثقيلة وتحميلها عند الطلب. لا يوجد سبب ظاهر لإعادة كتابة المشروع كله بإطار آخر.

## خطة تنفيذ مرتبة

| المرحلة | العمل | شرط القبول |
|---|---|---|
| 1. حماية الوصول | جلسات خادم وصلاحيات، حماية Sheets، أسرار Telegram وخطافات موثقة | المستخدم غير المخول لا يقرأ البيانات، والطلب المزور للـwebhook يرفض دون إرسال |
| 2. ضبط الأرقام | قاموس للمؤشرات والتواريخ والتكاليف، إصلاح الحالات والربح والمقارنات | نفس العينة تعطي نفس الأرقام في اللوحة والتقرير والبوت؛ الجاري لا يحتسب مسلّماً |
| 3. الاعتمادية | أخطاء ظاهرة وتحديث موحد، مزامنة وقاعدة بيانات، snapshots وتنبيهات خادم | إغلاق المتصفح لا يوقف الحفظ والتنبيهات؛ انقطاع المصدر لا يعرض أصفاراً مضللة |
| 4. العمليات والواجهة | تنظيم التنقل، تفاصيل الطلب والمتابعة، المنتجات والمتغيرات، تحصيل COD | المستخدم يعرف الإجراء التالي ويربط كل مبلغ بطلب وتسوية |
| 5. التحليلات | ربح حسب المنتج والولاية والقناة، تقييم مخاطر من نتائج فعلية | توضيح حجم العينة ودقة النموذج، ومقارنات بفترات وتعريفات متسقة |

أول حزمة إصلاحات عملية: توثيق Telegram والحماية، إزالة نسب النمو الثابتة، توحيد صافي الربح، إصلاح معدل التسليم السنوي والحالات، وإظهار أخطاء المزامنة وتحديث المصدرين.

## الصيانة والتحقق المستمر

- استثناء نسخة `dz-commerce` من Vitest وESLint أو نقلها لاحقاً بعد التأكد من غرضها؛ gitignore وحده لم يمنع أدوات الفحص من تضمينها. لا تحذفها دون مراجعة محتواها.
- إصلاح أخطاء ESLint الأساسية وعددها 46. تشمل imports غير مستعملة، مكونات معرفة أثناء render في Orders، وقواعد hooks؛ ليست كلها أخطاء تشغيل متساوية الخطورة.
- إضافة CI لتثبيت مضبوط بالـlockfile، وlint وTypeScript للواجهة وAPI، والاختبارات والبناء. README الحالي وثيقة قالب Vite وليس دليل المشروع.
- الاختبارات الحالية تمر لكنها لا تغطي أهم الثغرات الحسابية. بعض اختبارات UI تعيد كتابة المعادلات بدل تركيب المكون الحقيقي. الأولوية لاختبارات حالات وتواريخ ومعادلات ببيانات اصطناعية، وصلاحيات API، ومزامنة مع فشل وإعادة محاولة، ثم رحلة مستخدم للبحث والتصفية والتحديث.
- الاختبارات المطلوبة للحسابات: طلبات كلها قيد التوصيل، مرتجعات بشحن، صفر بيانات، شهر صغير وآخر كبير، الحالات العربية والفرنسية والإنجليزية، بداية اليوم بالجزائر، تاريخ غير صالح، وتكاليف ناقصة.
- توثيق متغيرات البيئة دون قيم سرية، ومعنى أعمدة Sheets، وطريقة المزامنة والنشر والتراجع واستعادة النسخ الاحتياطية. إضافة مراقبة أخطاء لا تسجل أسماء العملاء أو هواتفهم أو tokens.

قبل تنفيذ المعادلات والهجرة، تثبت أربعة تعريفات: معنى Total وDelivery، معنى تاريخ Tracking، مصدر الحقيقة بين النظام التجاري والشيت، وهل المرتجع يعود صالحاً للمخزون. هذه القرارات تمنع نقل الالتباس الحالي إلى قاعدة بيانات جديدة.
