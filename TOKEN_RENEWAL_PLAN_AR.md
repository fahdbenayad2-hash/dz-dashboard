# حل انتهاء مفاتيح Octomatic

تاريخ التحقق: 6 سبتمبر 2026. تم فحص ملف JavaScript العام المشار إليه من صفحة Octomatic، دون استخدام مفاتيح الحساب أو تنفيذ تسجيل دخول أو تغيير جلسة.

## نتائج مؤكدة

- Apps Script الحالي يحفظ JWT_TOKEN وX_AUTH_KEY في Script Properties ولا يحتوي آلية تجديد.
- كود واجهة Octomatic يستخدم POST إلى `https://leaderscod.com/tenants/login` مع حقول `phoneOrEmail` و`password` و`storeName` ورأس X-Authorization.
- واجهة الدخول تستخرج `token` من استجابة الدخول، وتختار نطاق المتجر من `data.tenant.stores` حسب store_name.
- فحص `/api/me` يعيد المستخدم إلى صفحة الدخول عند 401 أو 423.
- لم تظهر معرفات refresh_token أو refreshToken أو /refresh في ملف JavaScript الرئيسي المفحوص. هذا لا يثبت غيابها من الخادم أو من تكامل خاص يوفره المزود.
- X-Authorization يظهر كقيمة ثابتة في كود العميل العام المفحوص، بخلاف JWT الذي يأتي من استجابة الدخول. لذلك لا يوجد في هذا الدليل ما يثبت وجوب تجديد المفتاحين معاً كل 30 يوماً. لم تُحفظ القيمة في هذا التقرير.
- مدة 30 يوماً معلومة قدمها المستخدم؛ لم يتم فحص claims التوكن الحي لتأكيد مدة صلاحيته.

## التوصية

الأفضل إذا وفره المزود: حساب تكامل بصلاحية القراءة مع مفتاح مخصص أو آلية refresh رسمية. وجود مسار دخول مستعمل من الواجهة لا يجعله تلقائياً عقد API عام مضمون الاستقرار.

الحل المرشح وفق الكود المتاح: إعادة مصادقة من الخادم عند قرب انتهاء JWT، باستخدام حساب مخصص بأقل صلاحيات تسمح بقراءة الطلبات والتتبع، إذا كانت هذه الآلية مسموحة ومدعومة للحساب. لا يغيَّر exp داخل JWT؛ الخادم هو من يصدر التوكن الجديد.

## التصميم التنفيذي

1. وظيفة getValidAccessToken تقرأ التوكن المحفوظ وتستعمل exp، إن توفر، لجدولة التجديد فقط؛ فك payload ليس تحققاً من التوقيع أو دليلاً على صلاحية الوصول.
2. قبل انتهاء التوكن بهامش مناسب، تحصل الوظيفة على قفل وتعيد قراءة التوكن لتجنب عدة عمليات دخول متزامنة.
3. تنفذ تسجيل الدخول بالمسار والبنية المثبتين، عبر حساب مخصص، دون تسجيل جسم الطلب أو كلمة المرور أو التوكن في logs.
4. تتحقق من HTTP وبنية الرد ومن أن المتجر والنطاق يطابقان المتجر المسموح مسبقاً. لا تتبع نطاقاً غير متوقع وتعيد إرسال التوكن إليه.
5. تتحقق بطلب قراءة بسيط من أن الاعتماد الجديد يعمل، ثم تستبدل التوكن المحفوظ وتسجل وقت النجاح فقط. فشل التجديد لا يمسح البيانات القديمة.
6. عند 401 من طلب بيانات: إعادة مصادقة واحدة ثم إعادة الطلب مرة واحدة. عند 403/423 يتوقف التنفيذ ويُعرض احتياج مراجعة الصلاحيات أو الحساب، ولا يكرر الدخول بلا نهاية. 429 يعالج بالتأخير والحد من المحاولات.
7. إذا تطلب الدخول CAPTCHA أو OTP أو تحققاً يدوياً، تظهر حالة «يلزم إعادة ربط الحساب»؛ لا يمكن ضمان تجديد غير مراقب في هذه الحالة.
8. انتهاء المصادقة مستقل عن اكتمال المزامنة: تجديد JWT لا يصلح مشكلة timeout ذات الست دقائق، ويجب تنفيذ خطة الدفعات أيضاً.

## حفظ بيانات الحساب

لا توضع كلمة المرور في React أو Google Sheets أو Git أو المحادثة. تدخل مرة واحدة بواسطة مالك الحساب في مخزن أسرار الخدمة. مع Apps Script يمكن استعمال Script Properties كمرحلة انتقالية مع تقييد محرري المشروع؛ هذا ليس خزنة مستقلة عن صلاحيات محرري السكريبت. الأفضل في البنية اللاحقة مخزن أسرار للخادم مع صلاحيات محدودة.

## اختبار القبول قبل التفعيل

- اختبار واحد مصرح به للحساب المخصص للتحقق من شكل الرد والصلاحيات وتأثير إعادة الدخول على الجلسات الأخرى.
- التحقق من أن التجديد يعمل بعد انتهاء الاعتماد القديم، لا بمجرد نجاح الدخول الأول.
- محاكاة 401 و403 و423 و429 وفشل الشبكة ورد ناقص، وعدم حدوث حلقات إعادة دخول أو تسريب أسرار.
- محاكاة تشغيلين متزامنين والتأكد من إصدار/حفظ اعتماد واحد صالح.
- مراقبة انتهاء الصلاحية وآخر نجاح للتجديد وحالة تحتاج تدخلاً، دون كشف القيم السرية.

## نص جاهز للدعم — لم يُرسل

«لدينا لوحة تحليل داخلية تقرأ طلبات متجرنا وتتبع الشحن، وJWT الحالي يتطلب الاستبدال كل 30 يوماً. هل توفرون API key لحساب تكامل بصلاحية قراءة فقط أو refresh token رسمي؟ إذا لا، هل تدعمون إعادة المصادقة الآلية عبر tenants/login؟ وهل X-Authorization ثابت للتطبيق أم خاص بالحساب، وهل إصدار توكن جديد يبطل الجلسات السابقة؟ نحتاج أيضاً حدود الطلبات وسياسة 2FA للتكامل.»

الحالة: الحل محدد تقنياً، ولم يُفعّل تسجيل دخول آلي أو تُحفظ كلمة مرور. يلزم التحقق من طريقة الاعتماد المناسبة للحساب قبل النشر.
